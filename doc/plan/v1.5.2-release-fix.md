# wyj-code v1.5.2 可复现发布修复计划

> 状态：本机与固定 Rust 1.96 发布门禁已通过，待推送 tag 并验证远端 Release/Pages
>
> 版本：v1.5.2
>
> 日期：2026-08-02
>
> 不可移动基线：v1.4.4、v1.5.0、v1.5.1 annotated tag

## 1. 目标

保留 v1.5.0 完成的全部 P1/P2 和 v1.5.1 的国内模型多工具调用兼容修复，解决 GitHub 可变 `stable` 工具链导致的发布不可复现问题，并产出真实可下载的五平台 Release 资产。

## 2. 修复范围

- 按 Rust/Clippy 1.96 的等价建议重写四类 lint，不改变业务语义：Agent/Skill frontmatter、工具参数对象提取、MCP server 稳定排序、TUI import 确认。
- ToolSearch/lazy schema 始终加入已注册的核心读写、shell、协作与 Plan 退出工具；可选 MCP/集成仍按搜索结果懒加载，权限和注册范围不扩张。
- Release Test/Lint、五平台 Build 与 Review Action 明确固定 Rust 1.96.0。
- 本地仍保留 Rust 1.80 MSRV 声明；CI 工具链固定只用于可复现发布和严格 lint，不代表提高运行时功能需求。
- 官网、版本、锁文件、CHANGELOG 与发布计划统一为 v1.5.2，并保留失败 tag 的历史说明。

## 3. 门禁

```bash
cargo fmt --all -- --check
git diff --check
cargo check --workspace --locked
cargo test --workspace --locked
cargo clippy --workspace --all-targets --locked -- -D warnings
cargo metadata --locked --no-deps --format-version=1
node --check site/script.js
```

Linux 额外以 Rust 1.96.1 容器执行：

```bash
cargo clippy --offline --workspace --all-targets --locked -- -D warnings
```

并验证：

- 12 个 `wyj-*` workspace package 全部为 1.5.2。
- CLI 版本/help、三份 Workflow YAML、凭证模式检查和项目自身 Review scanner。
- v1.5.1 新增的两项多工具调用回归测试继续通过。

## 4. 远端完成定义

1. 推送简体中文发布提交和新的 annotated tag v1.5.2，不移动历史 tag。
2. Release 的 Test & Lint、五平台 Build、Publish Release 全部成功。
3. Release 包含五个 archive、五个 sidecar checksum 和 `SHA256SUMS` 共 11 个资产。
4. sidecar 与聚合 checksum 逐项一致，Release 为非 draft、非 prerelease。
5. Pages 显示 v1.5.2，并保留 v1.5.1、v1.5.0、v1.4.4 历史卡片。
6. master/origin/master 一致，v1.5.2 指向最终提交，所有旧 tag 解引用保持不变。

## 5. 发布前验证证据

- `cargo test --workspace --locked`：671 passed、1 ignored、0 failed；ignored 项为需真实公网条件的 macOS sandbox 网络专项。
- macOS 本机 `cargo clippy --workspace --all-targets --locked -- -D warnings`：通过，0 warnings。
- Linux Rust 1.96.0 与 1.96.1 容器等价严格 Clippy：均通过，0 warnings。
- `cargo fmt --all -- --check`、`git diff --check`、`cargo check --workspace --locked`：通过。
- 国内模型专项回归：多工具调用串行执行/重试计数，以及 lazy schema 核心执行面保底测试全部通过。
